"use strict"

import "./styles/main.scss"

import CookieConsent from "./models/CookieConsent"

/*
//template example
//replace value upto user setting and then generate js file for each user

const cc = new CookieConsent(
  {
    "palette": {
      "popup": {
        "background": "#000"
      },
      "button": {
        "background": "#f1d600"
      }
    },
    "theme": "classic"
  }
);
*/